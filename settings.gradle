pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()

        // jcenter() for outdated dependencies
        jcenter()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        // jcenter() for outdated dependencies
        jcenter()
    }
}
rootProject.name = "Face Recognition"
include ':app'
